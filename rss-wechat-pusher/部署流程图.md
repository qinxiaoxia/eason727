# RSS 推送到企业微信 - 完整部署流程图

---

## 如何查看流程图

当前文档使用 Mermaid 语法，若预览只显示代码、不显示图形，可用下面任一方式查看：

1. **GitHub**：把文件推送到仓库，在 GitHub 网页打开此 `.md` 文件，会自动渲染流程图  
2. **Mermaid Live**：打开 [mermaid.live](https://mermaid.live)，复制下方任意 `mermaid` 代码块粘贴进去即可看到图  
3. **Cursor / VS Code**：安装插件 [Markdown Preview Mermaid Support](https://marketplace.visualstudio.com/items?itemName=bierner.markdown-mermaid)，然后 `Cmd+Shift+V` 打开预览

---

## 一、数据源：从公众号到 RSS

![数据源流程图](https://mermaid.ink/img/Zmxvd2NoYXJ0IExSCiAgICBBW-WFrOS8l-WPt10gLS0-IEJbV2VXZSBSU1NdCiAgICBCIC0tPiBDW2FsbC5hdG9tXQogICAgRFvmma7pgJpSU1NdIC0tPiBFW0ZFRURTX0pTT05dCiAgICBDIC0tPiBF)

```mermaid
flowchart LR
    subgraph 公众号层
        MP1["国家网络安全通报中心"]
        MP2["国家互联网应急中心"]
        MP3["其他订阅公众号"]
    end

    subgraph WeWe_RSS["WeWe RSS（Zeabur 部署）"]
        W1["公众号 → RSS 转换"]
        W2["输出 all.atom"]
    end

    subgraph 普通RSS["普通 RSS 源"]
        R1["cybersecuritynews"]
        R2["helpnetsecurity"]
        R3["hackernews.cc"]
        R4["安蚁 / freebuf"]
    end

    MP1 --> W1
    MP2 --> W1
    MP3 --> W1
    W1 --> W2
    W2 --> FEEDS["FEEDS_JSON 配置"]
    R1 --> FEEDS
    R2 --> FEEDS
    R3 --> FEEDS
    R4 --> FEEDS
```

**说明**：
- **WeWe RSS**：独立服务，部署在 Zeabur，将微信公众号转为 RSS。用户在其后台订阅公众号后，输出 `https://xxx.zeabur.app/feeds/all.atom`
- **FEEDS_JSON**：每项为 `[feed_url, source_type]`，`source_type` 为 `wewe_rss`（公众号）或 `rss`（普通 RSS）
- **WEWE_RSS_URL**：用于识别公众号来源，规则分类（如监管机构预警）依赖作者/公众号名

---

## 二、整体部署架构

![部署架构图](https://mermaid.ink/img/Zmxvd2NoYXJ0IFRCCiAgICBXZVdlW1dlV2UgUlNTXSAtLT4gTWFpblttYWluLnB5XQogICAgUlNTW-aZrumAmlJTU10gLS0-IE1haW4KICAgIFRyaWdnZXJbWmVhYnVy6Kem5Y-R5ZmoXSAtLT4gQVBJW0dpdEh1YiBBUEldCiAgICBBUEkgLS0-IEFjdGlvbnNbQWN0aW9uc10KICAgIEFjdGlvbnMgLS0-IE1haW4KICAgIE1haW4gLS0-IERCWyhyc3NfcHVzaC5kYildCiAgICBNYWluIC0tPiBXZUNoYXRb5LyB5Lia5b6u5L-hXQ)

```mermaid
flowchart TB
    subgraph 数据源
        WeWe["WeWe RSS<br/>公众号聚合 all.atom"]
        RSS["普通 RSS<br/>网安站点"]
    end

    subgraph Zeabur["☁️ Zeabur"]
        Trigger["rss-push-trigger<br/>每 1 小时调用 GitHub API"]
    end

    subgraph GitHub["🐙 GitHub eason727"]
        API["repository_dispatch"]
        Actions["rss-push.yml"]
        Main["main.py<br/>拉取·分类·推送·去重"]
        DB[(rss_push.db)]
    end

    subgraph 输出
        WeChat["企业微信群<br/>Webhook"]
    end

    WeWe --> Main
    RSS --> Main
    Trigger --> API
    API --> Actions
    Actions --> Main
    Main --> DB
    Main --> WeChat
    Actions -->|"提交 DB"| DB
```

---

## 三、触发链路

![触发链路图](https://mermaid.ink/img/Zmxvd2NoYXJ0IExSCiAgICBBW1plYWJ1ciAxaF0gLS0-IERbQWN0aW9uc10KICAgIEJbR2l0SHViIENyb25dIC0tPiBECiAgICBDW-aJi-WKqF0gLS0-IEQKICAgIEQgLS0-IFJ1blttYWluLnB5XQ)

```mermaid
flowchart LR
    A["Zeabur 定时<br/>每 1 小时"] --> D["GitHub Actions"]
    B["GitHub Cron<br/>整点后 2 分钟"] --> D
    C["手动 workflow_dispatch"] --> D
    D --> Run["执行 main.py"]
```

---

## 四、main.py 完整执行流程

![main执行流程图](https://mermaid.ink/img/Zmxvd2NoYXJ0IFRECiAgICBTdGFydChb5byA5aeLXSkgLS0-IEZldGNoW-aLieWPllJTU10KICAgIEZldGNoIC0tPiBQYXJzZVvop6PmnpBdCiAgICBQYXJzZSAtLT4gQ2xhc3NpZnlb5YiG57G7XQogICAgQ2xhc3NpZnkgLS0-IFJlYWx0aW1le-WunuaXtuexuz99CiAgICBSZWFsdGltZSAtLT585pivfCBQdXNoUlvnq4vljbPmjqjpgIFdCiAgICBSZWFsdGltZSAtLT585ZCmfCBTY2hlZHVsZWR75a6a5pe256qX5Y-jP30KICAgIFNjaGVkdWxlZCAtLT585pivfCBQdXNoU1vlrprml7bmjqjpgIFdCiAgICBTY2hlZHVsZWQgLS0-fOWQpnwgRW5kKFvnu5PmnZ9dKQogICAgUHVzaFIgLS0-IERCW-WGmeWFpURCXQogICAgUHVzaFMgLS0-IERCCiAgICBEQiAtLT4gR2l0W-aPkOS6pERCXQogICAgR2l0IC0tPiBFbmQ)

```mermaid
flowchart TD
    Start([开始]) --> Fetch["并行拉取 FEEDS_JSON 所有源<br/>8 线程"]
    Fetch --> Parse["解析 entry：link、title、summary、author、published"]
    Parse --> Insert["写入 articles 表<br/>新文章去重"]
    Insert --> Classify["分类器 classify()"]

    Classify --> Rule{"规则命中？<br/>监管机构预警"}
    Rule -->|是| Alert["监管机构预警"]
    Rule -->|否| LLM{"配置 LLM？"}
    LLM -->|是| LLMCat["漏洞信息 / 重大安全事件 /<br/>网安新闻 / 网安赛事 / 其他"]
    LLM -->|否| Other["其他资讯"]
    Alert --> Realtime
    LLMCat --> Realtime
    Other --> Realtime

    Realtime{"实时类？<br/>监管预警 or 重大事件"}
    Realtime -->|是 + 当天| PushR["立即推送"]
    Realtime -->|否| Scheduled{"在 9:30 或 15:30 窗口？"}
    Scheduled -->|是| Filter["时间过滤"]
    Scheduled -->|否| End1([结束])
    Filter --> PushS["定时推送 6 类"]
    PushR --> MarkDB["写入 pushed 表"]
    PushS --> MarkDB
    MarkDB --> GitCommit["有变更则 commit + push rss_push.db"]
    GitCommit --> End2([结束])
```

---

## 五、需求模块详解

### 5.1 分类模块（6 类）

![分类模块图](https://mermaid.ink/img/Zmxvd2NoYXJ0IFRECiAgICBSMVvop4TliJk6IOmAmuaKpeS4reW_gyvph43ngrnpmLLojINdIC0tPiBBbGVydFvnm5HnrqHmnLrmnoTpooToraZdCiAgICBSMlvop4TliJk6IENOQ0VSVCvpo47pmanmj5DnpLpdIC0tPiBBbGVydAogICAgTExNW-inhOWImeacquWRveS4rS0-TExNXSAtLT4gQzFb5ryP5rSe5L-h5oGvXQogICAgTExNIC0tPiBDMlvph43lpKflronlhajkuovku7ZdCiAgICBMTE0gLS0-IEMzW-e9keWuieaWsOmXu10KICAgIExMTSAtLT4gQzRb572R5a6J6LWb5LqLXQogICAgTExNIC0tPiBDNVvlhbbku5botYTorq9d)

```mermaid
flowchart TD
    subgraph 规则分类
        R1["国家网络安全通报中心 + 标题「重点防范」"]
        R2["国家互联网应急中心CNCERT + 标题「关于」且含「风险提示」"]
        R1 --> Alert["监管机构预警"]
        R2 --> Alert
    end

    subgraph 大模型分类
        LLM["规则未命中 → 调用 LLM"]
        LLM --> C1["漏洞信息"]
        LLM --> C2["重大安全事件"]
        LLM --> C3["网安新闻资讯"]
        LLM --> C4["网安赛事资讯"]
        LLM --> C5["其他资讯"]
    end

    subgraph 兜底
        NoLLM["未配置 LLM → 全部归为「其他资讯」"]
    end
```

| 分类 | 说明 | 推送方式 |
|------|------|----------|
| 监管机构预警 | 规则命中 | 实时（仅当天） |
| 重大安全事件 | LLM 分类 | 实时（仅当天） |
| 漏洞信息 | LLM 分类 | 定时 9:30 / 15:30 |
| 网安新闻资讯 | LLM 分类 | 定时 |
| 网安赛事资讯 | LLM 分类 | 定时 |
| 其他资讯 | LLM 或兜底 | 定时 |

---

### 5.2 监管机构预警 - 重点防范（IOC 特殊格式）

![IOC流程图](https://mermaid.ink/img/Zmxvd2NoYXJ0IFRECiAgICBBW-mHjeeCuemYsuiMg-aWh-eroF0gLS0-IEJb5oqT5Y-W5q2j5paHXQogICAgQiAtLT4gQ1vmj5Dlj5ZJT0NdCiAgICBDIC0tPiBEe-acieaViElPQz99CiAgICBEIC0tPnzmmK98IEVb5Zu65a6a5qC85byP5o6o6YCBXQogICAgRCAtLT585ZCmfCBGW-aZrumAmuagvOW8j10)

```mermaid
flowchart TD
    Item["监管机构预警 + 国家网络安全通报中心 + 标题「重点防范」"]
    Item --> FetchBody["抓取文章正文<br/>BeautifulSoup 解析 js_content"]
    FetchBody --> Extract["正则提取 IOC<br/>恶意地址: xxx 关联IP: xxx<br/>或域名+IP 配对"]
    Extract --> Valid{"有效 IOC？<br/>排除 127.x、0.0.0.0"}
    Valid -->|是| Format["固定格式推送<br/>【信息类型】【标题】【时间】【链接】【相关IOC】"]
    Valid -->|否| Normal["并入普通 Markdown 格式"]
    Format --> WeChat
    Normal --> WeChat
```

---

### 5.3 推送策略模块

```mermaid
flowchart TD
    subgraph 实时推送
        R1["监管机构预警"]
        R2["重大安全事件"]
        R1 --> R3["仅当天文章<br/>_is_today_beijing()"]
        R2 --> R3
        R3 --> R4["去重后立即推送<br/>每类一条消息"]
    end

    subgraph 定时推送_930["9:30 北京"]
        T1["前一天 15:30 ~ 当天 9:30<br/>_is_in_930_window()"]
        T1 --> T3["全部 6 类合并推送"]
    end

    subgraph 定时推送_1530["15:30 北京"]
        T2["仅当天<br/>_is_today_beijing()"]
        T2 --> T3
    end

    subgraph 手动
        M1["--push-now 或 PUSH_SCHEDULED_NOW=1"]
        M1 --> M2["定时类按「仅当天」推送"]
    end
```

---

### 5.4 辅助模块

| 模块 | 说明 |
|------|------|
| **英文标题翻译** | 纯英文标题 → 调用 LLM 翻译 → 标题后加「（译）」 |
| **分片发送** | 单条超 3800 字节（约 4096 上限）→ 按换行分片，间隔 0.3s 防限流 |
| **去重** | `rss_push.db`：articles 表（已知链接）+ pushed 表（已推送），workflow 提交回仓库 |

---

## 六、数据流总览

![数据流总览图](https://mermaid.ink/img/Zmxvd2NoYXJ0IExSCiAgICBBW-WFrOS8l-WPt10gLS0-IEJbV2VXZSBSU1NdCiAgICBCIC0tPiBDW21haW4ucHldCiAgICBEW-aZrumAmlJTU10gLS0-IEMKICAgIEMgLS0-IEVb5YiG57G7XQogICAgRSAtLT4gRlvkvIHkuJrlvq7kv6Fd)

```mermaid
flowchart LR
    A["微信公众号"] --> B["WeWe RSS"]
    B --> C["all.atom"]
    C --> D["main.py"]
    E["普通 RSS"] --> D
    D --> F["分类"]
    F --> G["去重 + 时间过滤"]
    G --> H["企业微信"]
    D --> I["rss_push.db"]
    I --> J["workflow 提交"]
```

---

## 七、组件与配置

| 组件 | 位置 | 说明 |
|------|------|------|
| **WeWe RSS** | Zeabur（独立项目） | 公众号 → RSS，输出 all.atom |
| **rss-push-trigger** | Zeabur（独立仓库） | 每 1 小时调用 repository_dispatch |
| **rss-push.yml** | eason727/.github/workflows/ | 拉取、运行 main.py、提交 DB |
| **main.py** | eason727/rss-wechat-pusher/ | 拉取、分类、推送、去重 |
| **classifier.py** | 同上 | 规则 + LLM 分类 |
| **rss_push.db** | 仓库内 | 持久化去重，workflow 自动提交 |

### 环境变量 / Secrets

| 变量 | 位置 | 必填 | 说明 |
|------|------|------|------|
| GITHUB_TOKEN | Zeabur | 是 | PAT，repo 权限 |
| WECHAT_WEBHOOK | GitHub Secrets | 是 | 企业微信机器人 Webhook |
| FEEDS_JSON | GitHub Secrets | 是 | `[["url","wewe_rss"],["url","rss"],...]` |
| WEWE_RSS_URL | GitHub Secrets | 否 | WeWe RSS 地址，用于识别公众号 |
| LLM_API_KEY | GitHub Secrets | 否 | 大模型 API Key（DeepSeek/通义/智谱等） |
| LLM_BASE_URL | GitHub Secrets | 否 | API 地址 |
| LLM_MODEL | GitHub Secrets | 否 | 模型名 |
